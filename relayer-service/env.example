# EnderSwap HTLC Atomic Swap Demo Configuration

# =============================================================================
# Required Variables - You MUST configure these for the demo to work
# =============================================================================

# Your Base Sepolia wallet private key (without 0x prefix)
# Get Base Sepolia ETH from: https://www.coinbase.com/faucets/base-ethereum-sepolia-faucet
EVM_PRIVATE_KEY=your_base_sepolia_private_key_here

# Your Sui wallet private key in base64 format (must be exactly 32 bytes)
# Export this from your Sui wallet or generate with: sui keytool generate ed25519
SUI_PRIVATE_KEY=your_sui_private_key_base64_here

# Address of your deployed HTLC contract on Base Sepolia
# Deploy using: cd evm && forge create --rpc-url https://sepolia.base.org --private-key $EVM_PRIVATE_KEY src/HTLC.sol:HTLC
HTLC_ADDRESS=0x1234567890123456789012345678901234567890

# Package ID of your published HTLC module on Sui Testnet  
# Publish using: cd sui/htlc && sui client publish --gas-budget 20000000
SUI_HTLC_PACKAGE_ID=0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# =============================================================================
# Optional Variables - These have sensible defaults
# =============================================================================

# Amount of ETH to use in swaps (default: 0.01 ETH)
MAKER_AMOUNT=0.01

# Amount of SUI to use in swaps in MIST (default: 100000000 = 0.1 SUI)
# 1 SUI = 1,000,000,000 MIST
TAKER_AMOUNT=100000000

# =============================================================================
# Cross-Chain Destination Addresses (Optional - defaults to same wallet)
# =============================================================================

# For production swaps, specify different destination addresses
# If not provided, will use the same wallet addresses (demo mode)

# ETH address to receive funds in EVM→SUI swaps (taker's ETH destination)
TAKER_ETH_DESTINATION=

# SUI address to receive funds in EVM→SUI swaps (maker's SUI destination) 
MAKER_SUI_DESTINATION=

# ETH address to receive funds in SUI→EVM swaps (maker's ETH destination)
MAKER_ETH_DESTINATION=

# SUI address to receive funds in SUI→EVM swaps (taker's SUI destination)
TAKER_SUI_DESTINATION=

# =============================================================================
# Getting Test Funds
# =============================================================================

# Base Sepolia ETH Faucet:
# https://www.coinbase.com/faucets/base-ethereum-sepolia-faucet

# Sui Testnet Faucet:  
# https://docs.sui.io/guides/developer/getting-started/get-coins
# Or use: sui client faucet

# =============================================================================
# Contract Deployment Examples
# =============================================================================

# Deploy EVM HTLC Contract:
# cd evm
# forge create --rpc-url https://sepolia.base.org --private-key $EVM_PRIVATE_KEY src/HTLC.sol:HTLC

# Publish Sui HTLC Module:
# cd sui/htlc  
# sui client publish --gas-budget 20000000

# =============================================================================
# Wallet Setup Examples
# =============================================================================

# Generate new Sui keypair:
# sui keytool generate ed25519

# Export existing Sui wallet private key:
# sui keytool export <address> --key-identity <key-identity>

# =============================================================================
# Security Notes
# =============================================================================

# IMPORTANT: 
# - Never commit your private keys to version control
# - Only use testnet funds for this demo
# - Keep your private keys secure and never share them
# - The .env file should be added to .gitignore 